Goal
- Make the installed `goldencheck --version` path work.
- Add manifest verification for the version output so standard CLI metadata does not regress.

Approach
- Add Clap package version metadata in `crates/goldencheck/src/args.rs`.
- Add `--version` to the manifest-driven CLI help checks.
- Bump both published packages to `0.1.2`, because `0.1.1` was already published without the version fix.

Key decisions
- Use Clap's package version support instead of a custom flag.
- Do not change command behavior beyond standard version metadata.

Files to modify
- `crates/goldencheck/src/args.rs`
- `.plans/2026-05-13-150929-goldencheck-architecture.md.manifest.toml`
- `crates/goldencheck/Cargo.toml`
- `crates/goldencheck-install/Cargo.toml`
- `Cargo.lock`
