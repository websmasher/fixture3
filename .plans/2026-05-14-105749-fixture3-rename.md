Goal
- Rename the public project to `fixture3`.
- Rename the GitHub repository remote to `websmasher/fixture3`.
- Keep the local root directory unchanged for this session.
- Rewrite human documentation so installation appears immediately after a one-line purpose statement.
- Explain the fixture-first approval testing philosophy for agent-managed codebases.

Approach
- Rename Cargo packages, binary name, release assets, install stub, and repository metadata.
- Rename internal crate directories and package metadata to `fixture3`.
- Rename the default manifest to `fixture3.yaml`.
- Rename the generated state directory to `.fixture3`.
- Update verifier scripts and manifests so `scripts/verify-all.sh` remains the completion gate.
- Rewrite `README.md` around install, `fixture3 --help`, philosophy, workflow, and command reference.
- Rename the GitHub repository with `gh repo rename fixture3`.
- Update the local git remote to `https://github.com/websmasher/fixture3.git`.

Key decisions
- Preserve `behavior/golden` and `approved.normalized.json` terminology because "golden output" and "approved output" are the domain terms for the accepted output, not the product name.
- Keep historical worklogs as historical context while updating product references.
- Do not rename the local root folder.

Files to modify
- `Cargo.toml`
- `Cargo.lock`
- `README.md`
- `AGENTS.md`
- `release-plz.toml`
- `.github/workflows/ci.yml`
- `.github/workflows/binary-release.yml`
- `scripts/verify-all.sh`
- `scripts/verify-layer-6-fixture3.sh`
- `scripts/verify-manifest.py`
- `scripts/self-check-harness.py`
- `fixture3.yaml`
- `crates/fixture3/**`
- `crates/fixture3-install/**`
- `.plans/2026-05-13-150929-fixture3-architecture.md.manifest.toml`
- Self fixture manifests and approved self output.

Verification
- `scripts/verify-all.sh`
- `cargo install --path crates/fixture3 --force --locked`
- `fixture3 --version`
- GitHub repo URL resolves after rename.
