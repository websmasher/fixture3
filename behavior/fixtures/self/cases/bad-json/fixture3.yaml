version: 1
suites:
  case:
    fixtures:
      - "behavior/fixtures/self/cases/bad-json/input.txt"
    command:
      argv:
        - "python3"
        - "scripts/self-case-bad-json.py"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: "behavior/fixtures/self/cases/bad-json/approved"
      received_dir: ".fixture3/self-cases/bad-json"
      diff_dir: ".fixture3/self-cases/bad-json"
