version: 1
suites:
  case:
    fixtures:
      - "behavior/fixtures/self/cases/diff-existing/input.txt"
    command:
      argv:
        - "python3"
        - "scripts/self-case-emit-json.py"
        - "{fixtures}"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: "behavior/fixtures/self/cases/diff-existing/golden"
      received_dir: ".fixture3/self-cases/diff-existing"
      diff_dir: ".fixture3/self-cases/diff-existing"
