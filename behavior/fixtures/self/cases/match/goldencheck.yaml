version: 1
suites:
  case:
    fixtures:
      - "behavior/fixtures/self/cases/match/input.txt"
    command:
      argv:
        - "python3"
        - "scripts/self-case-emit-json.py"
        - "{fixtures}"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: "behavior/fixtures/self/cases/match/golden"
      received_dir: ".goldencheck/self-cases/match"
      diff_dir: ".goldencheck/self-cases/match"
