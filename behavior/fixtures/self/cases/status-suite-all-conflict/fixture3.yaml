version: 1
suites:
  alpha:
    fixtures:
      - "behavior/fixtures/self/cases/status-suite-all-conflict/input.txt"
    command:
      argv:
        - "python3"
        - "scripts/self-case-emit-json.py"
        - "{fixtures}"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: "behavior/fixtures/self/cases/status-suite-all-conflict/approved/alpha"
      received_dir: ".fixture3/self-cases/status-suite-all-conflict/alpha"
      diff_dir: ".fixture3/self-cases/status-suite-all-conflict/alpha"
