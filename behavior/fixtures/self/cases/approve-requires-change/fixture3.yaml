version: 1
suites:
  case:
    fixtures:
      - "behavior/fixtures/self/cases/approve-requires-change/input.txt"
    command:
      argv:
        - "python3"
        - "scripts/self-case-emit-json.py"
        - "{fixtures}"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: ".fixture3/self-cases/approve-requires-change/golden"
      received_dir: ".fixture3/self-cases/approve-requires-change"
      diff_dir: ".fixture3/self-cases/approve-requires-change"
