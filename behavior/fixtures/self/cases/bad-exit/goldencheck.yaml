version: 1
suites:
  case:
    fixtures:
      - "behavior/fixtures/self/cases/bad-exit/input.txt"
    command:
      argv:
        - "python3"
        - "scripts/self-case-exit-7.py"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: "behavior/fixtures/self/cases/bad-exit/golden"
      received_dir: ".goldencheck/self-cases/bad-exit"
      diff_dir: ".goldencheck/self-cases/bad-exit"
