version: 1
suites:
  alpha:
    fixtures:
      - "behavior/fixtures/self/cases/check-all-match/input-a.txt"
    command:
      argv:
        - "python3"
        - "scripts/self-case-emit-json.py"
        - "{fixtures}"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: "behavior/fixtures/self/cases/check-all-match/golden/alpha"
      received_dir: ".fixture3/self-cases/check-all-match/alpha"
      diff_dir: ".fixture3/self-cases/check-all-match/alpha"
  beta:
    fixtures:
      - "behavior/fixtures/self/cases/check-all-match/input-b.txt"
    command:
      argv:
        - "python3"
        - "scripts/self-case-emit-json.py"
        - "{fixtures}"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: "behavior/fixtures/self/cases/check-all-match/golden/beta"
      received_dir: ".fixture3/self-cases/check-all-match/beta"
      diff_dir: ".fixture3/self-cases/check-all-match/beta"
