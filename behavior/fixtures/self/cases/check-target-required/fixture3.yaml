version: 1
suites:
  alpha:
    fixtures:
      - "behavior/fixtures/self/cases/check-target-required/input.txt"
    command:
      argv:
        - "python3"
        - "scripts/self-case-emit-json.py"
        - "{fixtures}"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: "behavior/fixtures/self/cases/check-target-required/golden/alpha"
      received_dir: ".fixture3/self-cases/check-target-required/alpha"
      diff_dir: ".fixture3/self-cases/check-target-required/alpha"
