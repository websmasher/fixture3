version: 1
suites:
  self:
    fixtures:
      - "behavior/fixtures/self/cases/*/goldencheck.yaml"
    command:
      argv:
        - "python3"
        - "scripts/self-check-harness.py"
        - "{fixtures}"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: "behavior/golden/self"
      received_dir: ".goldencheck/self"
      diff_dir: ".goldencheck/self"
