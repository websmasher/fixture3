Summary
- Fixed the standard `fixture3 --version` CLI path after published `0.1.1` rejected it.
- Added manifest coverage for `--version` and bumped both packages to `0.1.2`.

Decisions made
- Used Clap's package version metadata instead of a custom version command.
- Released as `0.1.2` because `0.1.1` was already published without this fix.
- Kept the change scoped to CLI metadata and manifest verification.

Key files for context
- `crates/fixture3/src/args.rs`
- `.plans/2026-05-13-150929-fixture3-architecture.md.manifest.toml`
- `.plans/2026-05-13-212349-cli-version.md`
- `crates/fixture3/Cargo.toml`
- `crates/fixture3-install/Cargo.toml`

Verification
- `cargo check`
- `cargo run -p fixture3-cli -- --version`
- `cargo run -p fixture3-cli -- --help`
- `scripts/verify-all.sh`
- `cargo publish -p fixture3 --dry-run --allow-dirty`

Next steps
- Commit and push the `0.1.2` fix.
- Tag `v0.1.2`.
- Wait for GitHub binary release assets.
- Publish `fixture3 v0.1.2` to crates.io.
- Reinstall through `cargo binstall fixture3` and verify `fixture3 --help` and `fixture3 --version`.
