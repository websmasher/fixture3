Summary
- Fixed the standard `goldencheck --version` CLI path after published `0.1.1` rejected it.
- Added manifest coverage for `--version` and bumped both packages to `0.1.2`.

Decisions made
- Used Clap's package version metadata instead of a custom version command.
- Released as `0.1.2` because `0.1.1` was already published without this fix.
- Kept the change scoped to CLI metadata and manifest verification.

Key files for context
- `crates/goldencheck/src/args.rs`
- `.plans/2026-05-13-150929-goldencheck-architecture.md.manifest.toml`
- `.plans/2026-05-13-212349-cli-version.md`
- `crates/goldencheck/Cargo.toml`
- `crates/goldencheck-install/Cargo.toml`

Verification
- `cargo check`
- `cargo run -p goldencheck-cli -- --version`
- `cargo run -p goldencheck-cli -- --help`
- `scripts/verify-all.sh`
- `cargo publish -p goldencheck --dry-run --allow-dirty`

Next steps
- Commit and push the `0.1.2` fix.
- Tag `v0.1.2`.
- Wait for GitHub binary release assets.
- Publish `goldencheck v0.1.2` to crates.io.
- Reinstall through `cargo binstall goldencheck` and verify `goldencheck --help` and `goldencheck --version`.
