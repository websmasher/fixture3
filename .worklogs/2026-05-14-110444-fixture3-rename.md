Summary
- Renamed the project surface to `fixture3` while keeping the local root folder unchanged.
- Rewrote `README.md` so install instructions appear immediately after the one-line purpose statement, then added the approval-testing philosophy and workflow.
- Bumped local packages to `0.1.5` for the rename release.

Decisions made
- Kept `behavior/golden` as the approved-output directory name because that path stores accepted output, not the product name.
- Kept the crates.io package `fixture3` as the install stub and the implementation package as `fixture3-cli`.
- Updated historical plan and worklog content to the new product name so agents do not rediscover the old name from local text search.

Key files for context
- `README.md`
- `crates/fixture3/src/args.rs`
- `fixture3.yaml`
- `.plans/2026-05-13-150929-fixture3-architecture.md.manifest.toml`
- `behavior/golden/self/approved.meta.json`
- `.github/workflows/binary-release.yml`
- `.github/workflows/ci.yml`

Verification
- `cargo check`
- `scripts/verify-all.sh`
- Product-name denylist search across tracked project files.

Next steps
- Rename the GitHub repository to `websmasher/fixture3`.
- Push `main` and tag `v0.1.5`.
- Install the local `fixture3` binary from `crates/fixture3`.
